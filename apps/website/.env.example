# Public site URL for the latest extension release (GitHub Releases page).
# Exposed to the client via Vite — only use non-secret values.
VITE_RELEASES_URL=https://github.com/RashJrEdmund/remojis/releases/latest
